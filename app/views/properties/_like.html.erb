<%= link_to "" , proposals_path(property_id: property.id , type: "Like") ,
            :class => "like-#{property.id} btn btn-want btn btn-default btn-ghost btn-sm like",
            :remote => true,
            :method => :post %>